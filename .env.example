# Postgres connection string for the shared leaderboard (Neon / Vercel Postgres).
# Without it the app still runs — the leaderboard just reports "not configured".
# Local dev: copy to .env.local and fill in. Production: set in Vercel project env.
DATABASE_URL=
